# RUN: not lucid-c %s -emit=mlir 2>&1 | FileCheck %s
#
# Duplicate variable name in the same function scope.

def main() {
  var a = 1;
  var a = 2;
}

# CHECK: duplicated name used
# CHECK: module verification error
