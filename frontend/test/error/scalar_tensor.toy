# RUN: not lucid-c %s -emit=mlir 2>&1 | FileCheck %s
#
# Scalar + tensor element-wise add (no broadcast yet).

def main() {
  var a = 1 + [1, 2, 3];
}

# CHECK: requires the same type for all operands and results
# CHECK: module verification error
