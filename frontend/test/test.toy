def foo(a, b) {
    var h <3,2> = [1,2,3,4,5,6];
    return a + b + h;
}

def main() {

  var h <3,2> = [1,2,3,4,5,6];
  var g <2,3> = h;
  var i <2,3> = g;
  return foo(transpose(g), transpose(i));
}
