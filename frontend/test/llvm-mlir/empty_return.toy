# RUN: lucid-c %s -emit=LLVM 2>&1 | FileCheck %s
#
# Simplest possible: empty main function with return.
# Verifies basic LLVM dialect module structure.

def main() {
  return;
}

# CHECK: module {
# CHECK: llvm.func @main()
# CHECK: llvm.return
# CHECK: }
# CHECK-NOT: error:
